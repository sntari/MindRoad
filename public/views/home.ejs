<!-- -------------------------------- 로그인시 보여줄 화면 (챗봇) -------------------------- -->
<div class="chat-wrapper">
    <div class="chat-header">챗봇</div>
    <div class="chat-container">
        <div id="chatContent" class="chat-content">
            <!-- 메시지들이 여기에 동적으로 추가 -->
        </div>
    </div>
    <div class="input-container">
        <input type="text" id="chatInput" placeholder="메시지를 입력하세요..." />
        <button id="sendButton">전송</button>
    </div>
</div>
<!-- ------------------------------------------- 끝 -------------------------------------- -->

<!-- ----------------------------------- 로그인 안했을 때 화면 ----------------------------- -->
<div class="not_login_main_page">
    <div class="notChatbot">
        <div id="notLoginContent" class="not_login_content">
            <article>
                <p>test1</p>
            </article>
        </div>
    </div>
    <div class="notTarot">
        <div class="not_login_content">
            <article>
                <p>test2</p>
            </article>
        </div>
    </div>
    <div class="notGraph">
        <div class="not_login_content">
            <article>
                <p>test3</p>
            </article>
        </div>
    </div>
</div>
<!-- ------------------------------------------- 끝 --------------------------------------- -->

<!-- <script>
        var sections = $('.not_login_main_page > div'); // 모든 섹션을 가져옴
        var currentIndex = 0; // 현재 섹션의 인덱스
        var isAnimating = false; // 애니메이션 진행 중인지 확인
    
        $(window).on('wheel', function (event) {
            if (isAnimating) {
                return; // 애니메이션 중일 때 새로운 스크롤 이벤트를 막음
            }

            // 애니메이션 시작
            isAnimating = true;

            // 마우스 휠 방향에 따라 섹션을 변경
            if (event.originalEvent.deltaY < 0) {
                // 위로 스크롤 (이전 섹션으로 이동)
                if (currentIndex > 0) {
                    currentIndex--;
                }
            } else {
                // 아래로 스크롤 (다음 섹션으로 이동)
                if (currentIndex < sections.length - 1) {
                    currentIndex++;
                }
            }

            // 다음 섹션의 가운데로 부드럽게 스크롤 이동
            $('html, body').animate({
                scrollTop: $(sections[currentIndex]).offset().top
            }, 800, function() {
                // 애니메이션이 끝나면 다시 이벤트를 받을 수 있도록 설정
                isAnimating = false;
            });
        });
    </script> -->